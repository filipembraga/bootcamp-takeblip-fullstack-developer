import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <App />, //First parameter - WHAT*
  document.getElementById('root')  //Second parameter - WHERE*
);
